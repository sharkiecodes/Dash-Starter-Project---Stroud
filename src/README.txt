I consistently use the destructuring line 
{ store, onRemove, collection } = this.props

TypeScript looks at the props object and extracts 
the store, onRemove, and collection properties, creating three new variables store, onRemove, and collection.